/**
 * Created by XKTR67 on 4/19/2017.
 */

'use strict';
const chai = require('chai'),
    sinon = require('sinon'),
    rewire = require('rewire'),
    assert = sinon.assert,
    expect = chai.expect;
chai.should();

describe('[filters]',  () =>{
    let utils;
    beforeAll(() =>{
        utils = require('./../src/utils');
    });
    afterAll(() =>{
    });
    beforeEach(() =>{
    });
    afterEach(() =>{
    });
    it("utils", () =>{

    });
});